import type { PluginServerContext } from "@getpaseo/plugin/server";

export default function contribute(server: PluginServerContext) {
  // Server-side contributions, RPCs, or background watchers if needed
  return () => {};
}
